<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.jonathanpea.homeworknotes.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/addNote"
        android:name="com.jonathanpea.homeworknotes.addNote"
        android:label="fragment_blank"
        tools:layout="@layout/fragment_blank" />
    <activity
        android:id="@+id/homeworkActivity"
        android:name="com.jonathanpea.homeworknotes.HomeworkActivity"
        android:label="HomeworkActivity" />
    <action
        android:id="@+id/action_global_mainActivity"
        app:destination="@id/mainActivity" />
</navigation>